# Zuttocoin (ZTC) Node API

API Node backend untuk blockchain Zuttocoin (ZTC) yang menyediakan endpoint HTTP REST untuk:

- Broadcast transaksi: `POST /api/tx/send`
- Cek saldo alamat: `GET /api/address/{address}/balance`

---

## Instalasi

1. Clone repo ini  
2. Jalankan:  
